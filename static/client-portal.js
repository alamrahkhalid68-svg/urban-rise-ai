(()=>{const ease=t=>1-Math.pow(1-t,3);function animate(el,target,format){const start=performance.now(),duration=1100;function frame(now){const p=Math.min(1,(now-start)/duration),v=Math.round(target*ease(p));el.textContent=format?new Intl.NumberFormat('ar-SA').format(v):v;if(p<1)requestAnimationFrame(frame)}requestAnimationFrame(frame)}document.querySelectorAll('[data-count]').forEach(el=>animate(el,Number(el.dataset.count)||0,false));document.querySelectorAll('[data-money]').forEach(el=>animate(el,Number(el.dataset.money)||0,true));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.panel,.metrics article,.notice').forEach(el=>observer.observe(el));})();
